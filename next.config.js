/** @type {import('next').NextConfig} */

const nextConfig = {
    output: 'export',
    basePath: '/Projet_en_Etat_site',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;